import { createHash } from "crypto";
import { NextRequest, NextResponse } from "next/server";
import { authenticateAgent } from "@/lib/router-agent";
import { decryptSecret } from "@/lib/telegram-crypto";
import { dbGet, dbPatch } from "@/lib/telegram-db";
export const runtime="nodejs";export const dynamic="force-dynamic";
type Tok={id:string;network_id:string;telegram_user_id:number;purpose:string;token_hash:string;expires_at:string;used_at?:string|null};type Net={id:string;telegram_user_id:number;connection_mode:string;agent_secret_ciphertext?:string|null;agent_privileged_enabled?:boolean};
function hash(v:string){return createHash("sha256").update(v).digest("hex");}
function q(v:string){return v.replace(/\\/g,"\\\\").replace(/"/g,'\\"');}
function script(network:string,token:string,origin:string){const poll=`${origin}/api/router-agent/user-admin/poll?network=${encodeURIComponent(network)}&token=${encodeURIComponent(token)}`;const secret=`${origin}/api/router-agent/user-admin/secret?network=${encodeURIComponent(network)}&token=${encodeURIComponent(token)}&id=`;const result=`${origin}/api/router-agent/user-admin/result?network=${encodeURIComponent(network)}&token=${encodeURIComponent(token)}`;const activate=`${origin}/api/router-agent/user-admin/install?mode=activate&network=${encodeURIComponent(network)}&token=${encodeURIComponent(token)}`;return `:do { /system scheduler remove [find name="MT-TG-USER-ADMIN"] } on-error={}\n:do { /system script remove [find name="MT-TG-USER-ADMIN"] } on-error={}\n/system script add name="MT-TG-USER-ADMIN" policy=read,write,test,sensitive source={\n    :local pollUrl "${q(poll)}"\n    :local secretBase "${q(secret)}"\n    :local resultUrl "${q(result)}"\n    :local r [/tool fetch url=$pollUrl as-value output=user]\n    :local data ($r->"data")\n    :if (($data = "") || ($data = "NONE")) do={ :return }\n    :local p1 [:find $data "|"]\n    :if ($p1 = nil) do={ :return }\n    :local s [:pick $data ($p1 + 1) [:len $data]]\n    :local p2 [:find $s "|"]\n    :if ($p2 = nil) do={ :return }\n    :local cmdId [:pick $s 0 $p2]\n    :set s [:pick $s ($p2 + 1) [:len $s]]\n    :local p3 [:find $s "|"]\n    :local kind $s\n    :local args ""\n    :if ($p3 != nil) do={ :set kind [:pick $s 0 $p3]; :set args [:pick $s ($p3 + 1) [:len $s]] }\n    :local a1 ""; :local a2 ""; :local a3 ""; :local a4 ""\n    :if ([:len $args] > 0) do={ :local x [:find $args "|"]; :if ($x = nil) do={ :set a1 $args } else={ :set a1 [:pick $args 0 $x]; :set args [:pick $args ($x + 1) [:len $args]]; :set x [:find $args "|"]; :if ($x = nil) do={ :set a2 $args } else={ :set a2 [:pick $args 0 $x]; :set args [:pick $args ($x + 1) [:len $args]]; :set x [:find $args "|"]; :if ($x = nil) do={ :set a3 $args } else={ :set a3 [:pick $args 0 $x]; :set a4 [:pick $args ($x + 1) [:len $args]] } } } }\n    :local body ("id=" . $cmdId . "\\nstatus=error\\nerror=unsupported")\n    :local ver [/system resource get version]\n    :local major [:pick $ver 0 1]\n    :local profiles ""\n    :local source ""\n    :local customer ""\n    :if (($kind = "META") || ($kind = "PREVIEW")) do={\n        :do {\n            :if ($major = "7") do={\n                :do { :foreach p in=[/user-manager profile find] do={ :local nm [/user-manager profile get $p name]; :if ([:len $profiles] > 0) do={ :set profiles ($profiles . ",") }; :set profiles ($profiles . $nm) }; :if ([:len $profiles] > 0) do={ :set source "user-manager-v7" } } on-error={}\n            } else={\n                :do { :foreach p in=[/tool user-manager profile find] do={ :local nm [/tool user-manager profile get $p name]; :if ([:len $profiles] > 0) do={ :set profiles ($profiles . ",") }; :set profiles ($profiles . $nm) }; :if ([:len $profiles] > 0) do={ :set source "user-manager-v6"; :local cs [/tool user-manager customer find]; :if ([:len $cs] > 0) do={ :do { :set customer [/tool user-manager customer get [:pick $cs 0] login] } on-error={ :set customer "admin" } } } } on-error={}\n            }\n            :if ([:len $profiles] = 0) do={ :do { :foreach p in=[/ip hotspot user profile find] do={ :local nm [/ip hotspot user profile get $p name]; :if ([:len $profiles] > 0) do={ :set profiles ($profiles . ",") }; :set profiles ($profiles . $nm) }; :if ([:len $profiles] > 0) do={ :set source "hotspot" } } on-error={} }\n            :if ($kind = "META") do={ :set body ("id=" . $cmdId . "\\nstatus=ok\\nsource=" . $source . "\\ncustomer=" . $customer . "\\nprofiles=" . $profiles . "\\nversion=" . $ver) } else={\n                :local uname $a1; :local found 0; :local profile "-"; :local disabled 0; :local active [:len [/ip hotspot active find where user=$uname]]\n                :local hu [/ip hotspot user find where name=$uname]\n                :if ([:len $hu] > 0) do={ :set found 1; :set source "hotspot"; :local u [:pick $hu 0]; :do { :set profile [/ip hotspot user get $u profile] } on-error={}; :do { :if ([/ip hotspot user get $u disabled] = true) do={ :set disabled 1 } } on-error={} } else={\n                    :if ($major = "7") do={ :do { :local uu [/user-manager user find where name=$uname]; :if ([:len $uu] > 0) do={ :set found 1; :set source "user-manager-v7"; :local u [:pick $uu 0]; :do { :set profile [/user-manager user get $u group] } on-error={}; :do { :if ([/user-manager user get $u disabled] = true) do={ :set disabled 1 } } on-error={} } } on-error={} } else={ :do { :local uu [/tool user-manager user find where username=$uname]; :if ([:len $uu] > 0) do={ :set found 1; :set source "user-manager-v6"; :local u [:pick $uu 0]; :do { :set profile [/tool user-manager user get $u actual-profile] } on-error={}; :do { :if ([/tool user-manager user get $u disabled] = true) do={ :set disabled 1 } } on-error={} } } on-error={} }\n                }\n                :set body ("id=" . $cmdId . "\\nstatus=ok\\nfound=" . $found . "\\nsource=" . $source . "\\ncustomer=" . $customer . "\\nprofiles=" . $profiles . "\\nprofile=" . $profile . "\\ndisabled=" . $disabled . "\\nactive=" . $active)\n            }\n        } on-error={ :set body ("id=" . $cmdId . "\\nstatus=error\\nerror=preview-failed") }\n    }\n    :if (($kind = "CHANGE_PROFILE") || ($kind = "CHANGE_PASSWORD") || ($kind = "DELETE_USER") || ($kind = "ADD_USER")) do={\n        :do {\n            :local uname $a1; :local src $a2; :local changed 0\n            :if ($kind = "CHANGE_PROFILE") do={ :local np $a3; :if ($src = "hotspot") do={ :local uu [/ip hotspot user find where name=$uname]; :if ([:len $uu] > 0) do={ /ip hotspot user set [:pick $uu 0] profile=$np; :set changed 1 } }; :if ($src = "user-manager-v6") do={ /tool user-manager user create-and-activate-profile customer=$a4 numbers=$uname profile=$np; :set changed 1 }; :if ($src = "user-manager-v7") do={ /user-manager user-profile add user=$uname profile=$np; :set changed 1 } }\n            :if ($kind = "CHANGE_PASSWORD") do={ :local sr [/tool fetch url=($secretBase . $cmdId) as-value output=user]; :local pwd ($sr->"data"); :if ([:len $pwd] = 0) do={ :error "secret-empty" }; :if ($src = "hotspot") do={ :local uu [/ip hotspot user find where name=$uname]; /ip hotspot user set [:pick $uu 0] password=$pwd; :set changed 1 }; :if ($src = "user-manager-v6") do={ :local uu [/tool user-manager user find where username=$uname]; /tool user-manager user set [:pick $uu 0] password=$pwd; :set changed 1 }; :if ($src = "user-manager-v7") do={ :local uu [/user-manager user find where name=$uname]; /user-manager user set [:pick $uu 0] password=$pwd; :set changed 1 } }\n            :if ($kind = "DELETE_USER") do={ :local dc 0; :foreach x in=[/ip hotspot active find where user=$uname] do={ /ip hotspot active remove $x; :set dc ($dc + 1) }; :if ($src = "hotspot") do={ :local uu [/ip hotspot user find where name=$uname]; :if ([:len $uu] > 0) do={ /ip hotspot user remove [:pick $uu 0]; :set changed 1 } }; :if ($src = "user-manager-v6") do={ :local uu [/tool user-manager user find where username=$uname]; :if ([:len $uu] > 0) do={ /tool user-manager user remove [:pick $uu 0]; :set changed 1 } }; :if ($src = "user-manager-v7") do={ :local uu [/user-manager user find where name=$uname]; :if ([:len $uu] > 0) do={ /user-manager user remove [:pick $uu 0]; :set changed 1 } }; :set body ("id=" . $cmdId . "\\nstatus=ok\\nchanged=" . $changed . "\\ndisconnected=" . $dc) }\n            :if ($kind = "ADD_USER") do={ :local np $a3; :local sr [/tool fetch url=($secretBase . $cmdId) as-value output=user]; :local pwd ($sr->"data"); :if ([:len $pwd] = 0) do={ :error "secret-empty" }; :if ($src = "hotspot") do={ /ip hotspot user add name=$uname password=$pwd profile=$np disabled=no; :set changed 1 }; :if ($src = "user-manager-v6") do={ /tool user-manager user add customer=$a4 username=$uname password=$pwd shared-users=1 disabled=no; :do { /tool user-manager user create-and-activate-profile customer=$a4 numbers=$uname profile=$np } on-error={ :local uu [/tool user-manager user find where username=$uname]; :if ([:len $uu] > 0) do={ /tool user-manager user remove [:pick $uu 0] }; :error "profile-assign-failed" }; :set changed 1 }; :if ($src = "user-manager-v7") do={ /user-manager user add name=$uname password=$pwd shared-users=1 disabled=no; :do { /user-manager user-profile add user=$uname profile=$np } on-error={ :local uu [/user-manager user find where name=$uname]; :if ([:len $uu] > 0) do={ /user-manager user remove [:pick $uu 0] }; :error "profile-assign-failed" }; :set changed 1 } }\n            :if ($kind != "DELETE_USER") do={ :set body ("id=" . $cmdId . "\\nstatus=ok\\nchanged=" . $changed) }\n        } on-error={ :set body ("id=" . $cmdId . "\\nstatus=error\\nerror=user-admin-write-failed") }\n    }\n    :do { /tool fetch url=$resultUrl http-method=post http-data=$body keep-result=no } on-error={ :log warning "MT-TG user admin result failed" }\n}\n/system scheduler add name="MT-TG-USER-ADMIN" interval=15s start-time=startup on-event="/system script run MT-TG-USER-ADMIN" policy=read,write,test,sensitive\n/system script run MT-TG-USER-ADMIN\n:do { /tool fetch url="${q(activate)}" keep-result=no } on-error={ :log warning "MT-TG user admin activation failed" }\n`;}
async function install(req:NextRequest){const network=req.nextUrl.searchParams.get("network")||"";const install=req.nextUrl.searchParams.get("install")||"";if(!network||!install)return new NextResponse("missing",{status:400});const t=(await dbGet<Tok>("tg_agent_install_tokens",{network_id:`eq.${network}`,purpose:"eq.user-admin",token_hash:`eq.${hash(install)}`,used_at:"is.null",select:"*",order:"created_at.desc",limit:"1"}))[0];if(!t||new Date(t.expires_at).getTime()<Date.now())return new NextResponse("installer token expired or invalid",{status:401});const n=(await dbGet<Net>("tg_networks",{id:`eq.${network}`,telegram_user_id:`eq.${t.telegram_user_id}`,connection_mode:"eq.agent",select:"id,telegram_user_id,connection_mode,agent_secret_ciphertext,agent_privileged_enabled",limit:"1"}))[0];if(!n?.agent_secret_ciphertext||!n.agent_privileged_enabled)return new NextResponse("privileged agent required",{status:409});const secret=decryptSecret(n.agent_secret_ciphertext);await dbPatch("tg_agent_install_tokens",{used_at:new Date().toISOString()},{id:`eq.${t.id}`,used_at:"is.null"});return new NextResponse(script(network,secret,req.nextUrl.origin),{headers:{"content-type":"text/plain; charset=utf-8","cache-control":"no-store"}});}
async function activate(req:NextRequest){const network=req.nextUrl.searchParams.get("network")||"";const token=req.nextUrl.searchParams.get("token")||"";const n=await authenticateAgent(network,token);if(!n)return new NextResponse("unauthorized",{status:401});const now=new Date().toISOString();await dbPatch("tg_networks",{agent_user_admin_enabled:true,agent_user_admin_installed_at:now,agent_user_admin_version:"user-admin-v1",updated_at:now},{id:`eq.${network}`});return new NextResponse("USER_ADMIN_OK");}
export async function GET(req:NextRequest){return req.nextUrl.searchParams.get("mode")==="activate"?activate(req):install(req);}
